version: 2
jobs:
  build:
    docker:
      - image: debian:stretch

    steps:
      - checkout
      - run: apt install curl -y
      - run: 
          name: Get Buildroot LTS
          command: cd ..;curl https://buildroot.org/downloads/buildroot-2019.02.6.tar.bz2 
      - run: ls -al 